# 现场经验边界记录

## 证据性质

[经验落地 / 边界声明]

用户已确认文章来自“一次接口超时排查”，并确认采用“排查故事线”。但当前材料还没有提供可公开复述的生产日志、抓包、监控截图、真实错误码、具体云厂商或连接池配置。

## 可写入正文的部分

- 可以写“这类问题最开始很容易被当成下游接口慢或应用 timeout 配置问题”。
- 可以写“转折点是把连接生命周期画出来，而不是继续堆应用日志”。
- 可以写“5 分钟级 NAT/LB idle timeout 与 2 小时级 TCP keepalive 首次探测之间存在时间窗错配”。

## 不应写入正文的部分

- 不写真实服务名、域名、IP、客户名、接口路径、完整请求体。
- 不声称“现场确认 NAT idle timeout 就是 300 秒”，除非后续补充配置、抓包或日志证据。
- 不编造具体故障时间、请求量、错误率、耗时百分位。
- 不把“静默杀死”写成所有中间设备的统一行为；有些设备会返回 RST，有些表现为黑洞 timeout。

## 后续如需补强 E1，可向用户确认的选择题

1. 当时应用层主要表现更接近哪一种？
   - A. 直接 timeout
   - B. broken pipe / connection reset
   - C. 首次失败后重试成功
   - D. 其他
2. 中间设备类型更接近哪一种？
   - A. NAT Gateway
   - B. Load Balancer
   - C. API Gateway / Proxy
   - D. 不确定
3. “5 分钟”来自哪里？
   - A. 真实配置
   - B. 抓包/日志推算
   - C. 云厂商默认值
   - D. 只是经验估计

## 当前处理结论

E1 暂不写成完整生产故事，只作为叙事骨架使用；正文中的事实强度由 E2-E6 和 R1/R2 支撑。
