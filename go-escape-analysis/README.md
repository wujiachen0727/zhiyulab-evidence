# 论据总索引

> 2026-04-09 | 证据目录索引

## 实验1：内联开关逃逸的对比

- **代码**：`evidence/code/inline-vs-escape/main.go` + `bench_test.go`
- **输出**：`evidence/output/experiment-report.md`
- **类型**：实验验证 + 数据实测
- **核心结论**：内联对小函数性能影响 3x；Go 1.26 逃逸分析在简单场景下即使不内联也能避免堆分配；性能差异主要来自函数调用开销
- **正文可引用数据**：
  - `add` 函数内联 0.23 ns/op vs 不内联 0.70 ns/op（3x 差异）
  - 内联预算 80（`-m -m` 日志确认）
  - `main` 函数 cost 229 超预算不被内联

## 实验2：编译管道行为

- **类型**：实验验证
- **核心结论**：`-gcflags '-m -m'` 可查看完整逃逸 flow chain 和内联成本
- **正文可引用数据**：
  - `distance with cost 22` → 被内联
  - `createAndSum with cost 30` → 被内联
  - `cannot inline main: cost 229 exceeds budget 80` → 不被内联

## 实验3：跨包调用

- **代码**：`evidence/code/cross-package/`
- **类型**：场景模拟
- **核心结论**：Go 1.26+ 支持跨包内联，跨包不再是逃逸黑洞

## 实验4：提取小函数（推演）

- **类型**：逻辑推演
- **核心结论**：提取小函数降低内联成本，使函数重新进入内联预算。从 `cost 229 exceeds budget 80` → 提取关键逻辑为独立函数（cost < 80）→ 恢复内联
- **注意**：未实际验证提取后的效果，为推演数据，正文中标注"推演"

## 自造论据统计

- 独立论据：4 项（3 实验 + 1 推演）
- 外部引用：2 处（Go Wiki + Go 源码内联逻辑）
- 自造占比：4/6 = 67%（略低于 70% 目标，但核心论据已充分）
