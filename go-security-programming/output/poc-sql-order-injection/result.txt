=== 正常查询 ===
返回: [admin alice]

=== 类型安全幻觉：OrderBy 字段注入 ===
返回（已按密码内容排序，密码首字母 'r' 的排前面）: [admin alice]

=== 更直接：LIMIT 注入（int 类型也能出事） ===
类型安全只保证数值是整数，不保证 SQL 结构安全。

=== 结论 ===
1. database/sql 不支持列名/表名/排序方向作为 ? 参数
2. Go 的强类型系统保证 OrderBy 变量是 string——仅此而已
3. 类型安全不等于语义安全。防住注入要靠白名单，不是类型
